import {
  actionNames,
  initialize,
  makeSendGoal,
  makeCancelGoal,
  createStreamEventListener
} from "cycle-robot-drivers-async";

test("makeSendGoal", async done => {
  expect(1).toBe(1);
  done();
});
