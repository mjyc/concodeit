{
  "private": "true",
  "main": "index.html",
  "scripts": {
    "prestart": "rm -rf dist/public && mkdir -p dist && cp -r public dist/",
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "@tensorflow-models/posenet": "^2.1.3",
    "@tensorflow/tfjs-converter": "^1.2.5",
    "@tensorflow/tfjs-core": "^1.2.5",
    "cycle-robot-drivers-async": "file:../cycle-robot-drivers-async",
    "node-blockly": "1.0.36",
    "tabletrobotface-userstudy": "0.0.5"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ]
}
