{
  "private": "true",
  "name": "concodeit-async",
  "main": "index.html",
  "scripts": {
    "prestart": "rm -rf dist/programs && mkdir -p dist && cp -r programs dist/ && test -e src/settings.json || echo {} > src/settings.json",
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "devel": "rm -rf node_modules/cycle-robot-drivers-async && ln -s ../../cycle-robot-drivers-async node_modules/cycle-robot-drivers-async"
  },
  "author": "Michael Jae-Yoon Chung",
  "license": "MIT",
  "dependencies": {
    "cycle-robot-drivers-async": "0.0.6",
    "node-blockly": "1.0.36",
    "tabletrobotface-userstudy": "0.0.5",
    "util.promisify": "1.0.0"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "@cycle/time": "^0.19.0",
    "parcel-bundler": "^1.6.1",
    "xstream": "^11.11.0"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ]
}
